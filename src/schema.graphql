
type Author {
  name: String!
  id: String!
  posts: [Post]
}

type Post {
  id: String!
  title: String!
  author: Author
}

type User {
  id: String!
  email: String!
}

type Query {
  posts: [Post]
  authors: [Author]
  users: [User]
}

schema {
  query: Query
}